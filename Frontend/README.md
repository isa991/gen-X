# Frontend

Esse é um exemplo de Frontend em HTML puro que vai ser substituido por Next.js

Para acessar, é necessário fazer um servidor dentro desta pasta
```sh
python3 -m http.server 3000
# Porta 300 para diferenciar com a porta do servidor Backend (8000)
```